{
  "name": "jobline",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "echo \"Use app-level build scripts\"",
    "dev": "echo \"Use app-level dev scripts\""
  }
}